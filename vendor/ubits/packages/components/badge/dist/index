function _(m = {}) {
  const {
    content: l,
    size: n = "md",
    type: y,
    variant: u = "primary",
    style: e,
    absolute: B = !1,
    position: $ = "top-right",
    className: L = "",
    label: p,
    showLabel: h = !1,
    labelTypography: v = "ubits-body-md-regular"
  } = m, s = y || (l != null && l !== "" ? "number" : "dot"), C = [
    "ubits-badge",
    n !== "md" ? `ubits-badge--${n}` : "",
    s === "dot" ? "ubits-badge--dot" : "",
    s === "number" ? "ubits-badge--number" : "",
    `ubits-badge--${u}`,
    e ? `ubits-badge--${e}` : "",
    B ? "ubits-badge--absolute" : "",
    B && $ ? `ubits-badge--absolute-${$}` : "",
    L
  ].filter(Boolean).join(" "), t = s === "number" && l !== void 0 && l !== null ? String(l) : "", S = e && ["light", "neutral", "bold"].includes(e);
  console.log("🔵 BadgeProvider - renderBadge INICIO - TODOS LOS PARÁMETROS:", {
    options: m,
    content: l,
    contentType: typeof l,
    contentValue: l,
    size: n,
    type: y,
    typeReceived: y,
    variant: u,
    style: e,
    styleReceived: e,
    absolute: B,
    position: $,
    className: L,
    label: p,
    showLabel: h,
    labelTypography: v,
    badgeType: s,
    badgeContent: t,
    badgeContentType: typeof t,
    badgeContentValue: t,
    needsDot: S,
    isBold: e === "bold",
    isLight: e === "light",
    isNeutral: e === "neutral"
  });
  let o = "";
  if (S) {
    const a = {
      primary: "#0c5bef",
      secondary: "#5c646f",
      success: "#13BD74",
      warning: "#F6AD55",
      error: "#E53E3E",
      info: "#3182CE"
    }, d = String(u || "primary").toLowerCase().trim(), b = a[d] || a.primary;
    console.log("🔵 BadgeProvider - Dot color calculation:", {
      originalVariant: u,
      normalizedVariant: d,
      dotColor: b,
      availableVariants: Object.keys(a),
      variantExists: d in a
    });
    const r = e === "bold" ? b : "#ffffff", x = e === "bold" ? "#ffffff" : b;
    if (console.log("🔵 BadgeProvider - Dot color calculation (needsDot=true):", {
      style: e,
      variant: u,
      dotColor: b,
      dotBgColor: x,
      textColor: r,
      badgeType: s,
      isBold: e === "bold"
    }), s === "number" && t) {
      const i = n === "xs" ? "18px" : n === "sm" ? "20px" : n === "md" ? "22px" : "24px";
      if (e === "bold") {
        console.log("🔵 BadgeProvider - BOLD NUMBER - INICIO:", {
          style: e,
          badgeType: s,
          badgeContent: t,
          size: n,
          dotColor: b,
          contentType: typeof t,
          contentValue: t,
          contentString: String(t),
          isEmpty: !t || t.toString().trim() === ""
        });
        const g = `<span class="ubits-badge__dot ubits-badge__dot--number" style="width: ${i}; height: ${i}; min-width: ${i}; background-color: #ffffff; border-radius: 50%; flex-shrink: 0; display: inline-flex; align-items: center; justify-content: center; margin-right: 0; color: #ffffff; font-size: ${n === "xs" ? "10px" : n === "sm" ? "11px" : n === "md" ? "12px" : "13px"}; font-weight: 600; line-height: ${i}; padding: 0; margin: 0;">${t}</span>`;
        o = g, console.log("🔵 BadgeProvider - BOLD NUMBER - DETALLES:", {
          badgeContent: t,
          badgeContentType: typeof t,
          badgeContentString: String(t),
          numberDotSize: i,
          dotHtml: g,
          badgeInnerContent: o,
          badgeInnerContentLength: o.length,
          containsNumber: o.includes(String(t)),
          containsDot: o.includes("ubits-badge__dot"),
          finalHTML: o
        });
      } else
        o = `<span class="ubits-badge__dot ubits-badge__dot--number" style="width: ${i}; height: ${i}; min-width: ${i}; background-color: ${x}; border-radius: 50%; flex-shrink: 0; display: inline-flex; align-items: center; justify-content: center; margin-right: 0; color: ${r}; font-size: ${n === "xs" ? "10px" : n === "sm" ? "11px" : n === "md" ? "12px" : "13px"}; font-weight: 600; line-height: ${i}; padding: 0; margin: 0;">${t}</span>`;
      console.log("🔵 BadgeProvider - badgeInnerContent (number):", o);
    } else {
      const i = n === "xs" ? "6px" : n === "sm" ? "7px" : n === "md" ? "8px" : "10px", f = e === "bold" ? "#ffffff" : x;
      console.log("🔵 BadgeProvider - Dot HTML generation:", {
        style: e,
        dotBgColor: x,
        finalDotBgColor: f,
        dotSize: i,
        isBold: e === "bold",
        willBeWhite: f === "#ffffff"
      });
      const g = e === "bold" ? "#ffffff" : f;
      o = `<span class="ubits-badge__dot" style="width: ${i}; height: ${i}; background-color: ${g}; background: ${g}; border-radius: 50%; flex-shrink: 0; display: inline-block; margin-right: 0;"></span>`, console.log("🔵 BadgeProvider - badgeInnerContent (dot):", o), console.log("🔵 BadgeProvider - Dot final color:", { style: e, finalColor: g, finalDotBgColor: f, isBold: e === "bold" });
    }
  } else
    o = s === "dot" ? "" : t;
  const c = `<span class="${C}">${o}</span>`;
  if (console.log("🔵 BadgeProvider - HTML FINAL - ANTES DEL WRAPPER:", {
    style: e,
    badgeType: s,
    badgeHtml: c,
    badgeHtmlLength: c.length,
    badgeInnerContent: o,
    badgeInnerContentLength: o.length,
    classes: C,
    showLabel: h,
    label: p,
    willUseWrapper: e && ["light", "neutral", "bold"].includes(e)
  }), e && ["light", "neutral", "bold"].includes(e)) {
    if (h) {
      const d = p || t || "";
      if (d) {
        const r = `<div class="ubits-badge-wrapper">
          ${c}
          <span class="${v}" ${e === "bold" ? 'style="color: var(--ubits-fg-on-accent, #ffffff) !important;"' : ""}>${d}</span>
        </div>`;
        return console.log("🔵 BadgeProvider - HTML FINAL - CON LABEL:", {
          style: e,
          badgeType: s,
          finalHtml: r,
          finalHtmlLength: r.length,
          containsNumber: r.includes(String(t || "")),
          containsDot: r.includes("ubits-badge__dot"),
          containsNumberText: r.includes("ubits-badge__number-text"),
          labelText: d
        }), r;
      }
    }
    const a = `<div class="ubits-badge-wrapper">
      ${c}
    </div>`;
    return console.log("🔵 BadgeProvider - HTML FINAL - SIN LABEL:", {
      style: e,
      badgeType: s,
      finalHtml: a,
      finalHtmlLength: a.length,
      containsNumber: a.includes(String(t || "")),
      containsDot: a.includes("ubits-badge__dot"),
      containsNumberText: a.includes("ubits-badge__number-text"),
      badgeContent: t
    }), a;
  }
  return p && h ? `<div class="ubits-badge-wrapper">
      ${c}
      <span class="${v}">${p}</span>
    </div>` : c;
}
function D() {
  return '<span class="ubits-button__badge"></span>';
}
function H(m = {}) {
  const l = document.createElement("div");
  return l.innerHTML = _(m), l.querySelector(".ubits-badge");
}
export {
  H as createBadge,
  _ as renderBadge,
  D as renderButtonBadge
};
